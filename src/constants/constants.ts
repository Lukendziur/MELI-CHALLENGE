export const ROUTES = {
  ITEMS: '/items',
  BASE: '/',
}

// Used in adapters
export const AUTHOR = {
  name: 'Lucila',
  lastname: 'Kendziur',
}

// ?: In a real project, this url must be in .env!
export const API_BASE_URL = 'https://api.mercadolibre.com'
