import React from 'react'

import Error from '@/components/Error/Error'

// ? For UX purposes, the user have the option to go back to home and start again
const page = () => {
  return <Error />
}

export default page
